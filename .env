# === Mode Configuration ===
# Options: BACKTEST or LIVE
MODE=BACKTEST

# === Kite API Keys ===
KITE_API_KEY=your_kite_api_key_here
KITE_API_SECRET=your_kite_api_secret_here
KITE_ACCESS_TOKEN=your_kite_access_token_here  # Generated after login

# === Trading Configuration ===
INITIAL_BALANCE=2000000      # Starting balance for paper trading (increased for large positions)
SYMBOL=NIFTY
NIFTY_TOKEN=256265          # Instrument token for NIFTY
QTY=25                      # Shares per trade (reduced for affordability)

# === Strategy Configuration ===
# Available strategies: sma_crossover
# You can add more strategies by implementing BaseStrategy
STRATEGY_NAME=sma_crossover
STRATEGY_FAST_PERIOD=5       # Fast SMA period (for sma_crossover)
STRATEGY_SLOW_PERIOD=20      # Slow SMA period (for sma_crossover)

# === Live loop configuration ===
LIVE_INTERVAL_SECONDS=300    # Loop interval in seconds (default 5 min)
KILL_SWITCH_FILE=stop_bot   # Create this file to stop the live loop safely

# === Risk Management ===
STOP_LOSS_PERCENT=0.02      # Stop-loss threshold (2%)
TAKE_PROFIT_PERCENT=0.04    # Take-profit threshold (4%)

# === Logging / Backtest CSV ===
LOGS_DIR=logs
BACKTEST_TRADES_CSV=logs/backtest_trades_log.csv
LIVE_TRADES_CSV=logs/trades_log.csv

# === Data Source Configuration ===
# Options: API or FREE
# API = Use Kite Connect API (requires valid credentials)
# FREE = Use Yahoo Finance (free, no API key required)
DATA_SOURCE=FREE      # Change to API when you have valid Kite credentials

# === Historical Data Settings ===
# These control how the bot fetches data for backtesting
FORCE_DATA_REFRESH=false     # Set to true to always download fresh data
DATA_HISTORY_DAYS=30         # Number of days of historical data to fetch
DATA_INTERVAL=5m             # Candle interval: 1m, 5m, 15m, 1h, 1d
# Note: Data is automatically refreshed if older than 1 hour
# During market hours (9:15 AM - 3:30 PM IST), it refreshes more frequently